
name: Test Self-Hosted Runner

on:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on: self-hosted
    steps:
      - name: Print greeting
        run: echo "Hello from your self-hosted runner on Azure! 🎉"
